/*
Go has garbage collector that takes care of collecting all the variables that can't be referenced.
*/

package main

func main() {
	// fmt.Printf("%d ", runtime.MemStats.Alloc/1024)
	// runtime.SetFinalizer(obj, func (obj *Obj))
}
